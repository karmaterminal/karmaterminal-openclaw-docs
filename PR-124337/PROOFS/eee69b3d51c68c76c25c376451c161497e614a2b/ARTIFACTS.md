# Artifact inventory

The target subtree contains every source artifact locally:

- three row evidence documents;
- three identity receipts;
- three behavioral receipts;
- three durable SQLite projections;
- three bounded journals;
- the original harness;
- three bootstrap workflow/run summaries;
- source and target methods, identity records, manifest, file map, and checksums.

Historical execution files retain their original bytes and identities. Target
qualification files add no replacement behavioral evidence.
