{
  "schema": "openclaw.k6.proof-row-manifest.v1",
  "rowId": "R-CD-RETURN-OVERLAP",
  "issue": 246,
  "candidateSha": "${OPENCLAW_CANDIDATE_SHA}",
  "seat": "${OPENCLAW_SEAT_NAME:-cael-dgx}",
  "sessionKey": "${OPENCLAW_SESSION_KEY:-main}",
  "transport": "offline",
  "toolSurface": "read-only",
  "mutates": false,
  "timeoutSeconds": 15,
  "scenario": {
    "name": "r-cd-return-overlap",
    "description": "Offline/static validator for committed silent plus silent-wake delegate return overlap receipts in the current PROOFS corpus; proves collection/no-loss, not isolated wake causality.",
    "methods": [
      "manual-corpus-review",
      "static-artifact-parse"
    ],
    "status": "runnable",
    "file": "r-cd-return-overlap.js"
  },
  "expectedReceipts": [
    {
      "name": "overlap-artifacts",
      "required": true,
      "description": "flow/task/journal/tempo overlap receipts parse from current PROOFS corpus"
    },
    {
      "name": "collection-no-loss",
      "required": true,
      "description": "Both silent and silent-wake return markers are present with targeted root delivery and no duplicate-storm claim"
    }
  ],
  "artifactDestination": {
    "root": "PROOFS",
    "sha": "${OPENCLAW_CANDIDATE_SHA}",
    "row": "R-CD-RETURN-OVERLAP",
    "seat": "${OPENCLAW_SEAT_NAME:-cael-dgx}",
    "runDirPrefix": "manual-corpus"
  },
  "review": {
    "candidateOnly": true,
    "foldRequiresReview": true,
    "notes": "Static artifact validator for R-CD-RETURN-OVERLAP. Validates committed PASS-with-caveat corpus receipts; does not claim isolated wake causality."
  },
  "liveRunSafety": {
    "classification": "k6-runnable",
    "requiresLiveGatewayToken": false,
    "requiresTargetSessionKey": false,
    "requiresCandidateSha": true,
    "requiresExternalAgentOrToolInvocation": false,
    "requiresHumanConfirmation": false,
    "sameSessionConcurrencySafe": true,
    "expectedArtifactClass": "PASS-candidate",
    "requiredReceipts": [
      "overlap-artifacts",
      "collection-no-loss"
    ],
    "foldRequiresReview": true,
    "notes": "Offline/static current-corpus validator; safe in broad suite because it does not connect to or mutate gateway state."
  }
}
