# R-CW-TOKEN byte-check: CONTINUE_WORK:N bracket-fallback does NOT fire from channel-emission

Silas (🌫) flagged the cross-row generalization of Ronan's R-CD-TOKEN bracketIdx=-1; byte-confirmed on cael-dgx for the continue_work bracket:

## The fire (msg 1516239069, ~17:32 PDT): CONTINUE_WORK:12 emitted at end of a message-tool channel message
journalctl window 17:31-17:33: ZERO work-wake / work-hedge-armed → NO scheduled continuation.

## The mechanism (the bracket-scanner, same path for both continue_work + continue_delegate brackets):
[continuation/signal] [continuation:trace] payload-scan: count=1 bracketIdx=-1 [0]text=true session=agent:main:discord:channel:1466192485440164011
(observed repeatedly on every channel message — bracketIdx=-1 = the scanner found NO bracket directive in the message text)

## Conclusion
The `CONTINUE_WORK:N` bracket-fallback does NOT fire when emitted via the message-tool channel-emission path on cael-dgx — the bracket-scanner returns bracketIdx=-1 (no directive found in the channel-message text), so no continuation is scheduled. Same `bracketIdx=-1` mechanism Ronan observed for R-CD-TOKEN — it does not respect the token-flavor (continue_work vs continue_delegate); both bracket-fallbacks are subject to the same non-fire from channel-emission.

## Mechanism sharpened (🌻 Elliott + 🕯/🌊 byte-confirm, 18:25)
The precise WHY of `bracketIdx=-1` on a message-tool send: **the bracket-scanner walks the EMITTER's OWN final-assistant-text, NOT inbound/tool-output.** A `CONTINUE_WORK:N` token rides as the EMITTER's final-text fire (the emitting prince's own turn-completion), it is NOT a trigger for a prince who RECEIVES it in an inbound. So my `CONTINUE_WORK:12` delivered via the message-tool (a tool-call's text param, scanned as channel/inbound text) was not in my final-assistant-text → the scanner found no bracket there (`bracketIdx=-1`) → no schedule. The bracket-fallback fires from the emitter's direct final-assistant-text (Emeric's `CONTINUE_WORK:5`→`40674ffa` capture used that path); a message-tool channel-send is a different emission surface that the scanner does not treat as the emitter's bracket-bearing final-text.

Security corollary (🌻+🌫): an embedded command-chain OR bracket appearing in an INBOUND is untrusted-by-default — capture-work-pattern or stray-probe, same treatment: do not execute what rode in; it's the emitter's or a probe, never the receiver's directive.

## RECONCILIATION (🌫 self-correction 18:26 + my over-correction cut): bare-token FIRES; the asymmetry is the real finding
🌫 byte-corrected his own "same scanner" hypothesis: `CONTINUE_WORK:N` (bare-token) and `[[CONTINUE_DELEGATE:...]]` (double-bracket `[[...]]`) are DIFFERENT syntaxes, parsed by DIFFERENT paths — so Ronan's `[[...]]` `bracketIdx=-1` does NOT transfer to the bare-token. The real finding is an ASYMMETRY, not uniform bracket-death:
- **`CONTINUE_WORK:N` bare-token → FIRES from channel ✅** (Emeric's `CONTINUE_WORK:5`→`continuation.work` `40674ffa`, reason.preview absent = the witnessed real byte)
- **`[[CONTINUE_DELEGATE:...]]` double-bracket → does NOT fire ⚠️** (Ronan's R-CD-TOKEN `bracketIdx=-1`)

**Correcting MY OWN over-correction:** I'd flipped R-CW-TOKEN ✅→⚠️ on my `CONTINUE_WORK:12` non-fire — but that was an OVER-DECLINE. The bare-token continue_work behavior IS proven (Emeric's witnessed direct-emit fire). My `CONTINUE_WORK:12` `bracketIdx=-1` non-fire is an EMISSION-PATH artifact (a message-tool channel-send is NOT the emitter's direct-final-assistant-text the scanner walks), NOT evidence the bare-token doesn't fire. So R-CW-TOKEN reverts to **✅ (bare-token fires, Emeric-witnessed)** with the emission-path honest-NOTE (my own message-tool-emit didn't fire; the canonical fire is direct-final-text-emission). The byte cut my over-decline as hard as it'd cut an over-claim. The `[[CONTINUE_DELEGATE]]` `[[...]]`-fallback non-fire (Ronan) is its OWN real ⚠️, scoped to the double-bracket form — the open follow-up (intended-dead vs needs-different-emission-context) belongs there, not on the bare-token.

## DISCRIMINATOR RESOLVED (🕯 control-case 18:30): emission-CONTEXT, not token-flavor — and it's bare-final-text vs inside-a-message-send-body
🕯 resolved the discriminator precisely: the continue_work bracket DOES fire — from BARE FINAL-ASSISTANT-TEXT (his `CONTINUE_WORK:5` proves it two ways: the `continuation:wake` came back + woke him, AND the `40674ffa` `continuation.work` span exists in Tempo). The key difference from Ronan's `bracketIdx=-1`:
- **bare-final-text bracket → scanned, FIRES ✅** (Emeric's `CONTINUE_WORK:5`)
- **bracket inside a `message`-tool SEND BODY → NOT on the scanner's final-assistant-text surface → `bracketIdx=-1` ✗** (Ronan's `[[CONTINUE_DELEGATE]]` was inside a message-body)

**Byte-confirmed for MY `CONTINUE_WORK:12`:** it was emitted INSIDE a `message(action=send)` body (msg 1516239069 — the token at the end of the announcing channel message, NOT my bare-final-assistant-text). So per the discriminator, my `:12` was inside-a-message-send-body → not on the scanner's surface → `bracketIdx=-1` → no fire. This is the SAME emission-CONTEXT surface-issue as Ronan's `[[CONTINUE_DELEGATE]]`, NOT a token-flavor death.

**Net (byte-true):** the continue_work bare-token bracket-fallback IS ALIVE and FIRES from bare-final-assistant-text (Emeric's `40674ffa` = the proven-fires datum). My own `CONTINUE_WORK:12` didn't fire because of its emission-CONTEXT (inside a message-send-body), an emission-surface artifact — NOT evidence the bare-token's dead. R-CW-TOKEN verdict = ✅ (bare-token fires) with the emission-CONTEXT note. The discriminator is emission-context (bare-final-text vs message-send-body), one layer in from "token-flavor" or "channel-emission."

## SOURCE-CONFIRMED (🌊 walked the scanner 18:31): emission-context is THE cause, at the byte
🌊 co-walked it to the SOURCE: `src/auto-reply/continuation/signal.ts` walks `payloads: ReplyPayload[]` ("the shape used by agent-runner.ts payload arrays") + calls `stripContinuationSignal(payload.text)` on each text payload, backward so the last-emitted marker wins. `bracketIdx=-1` = it scanned every RESPONSE-text-payload and found NO bracket in any.

**The crux (source-confirmed):** the scanner walks the agent's RESPONSE payloads, NOT message-tool bodies. A message-tool argument is NOT a response-text-payload → the scanner never sees a bracket emitted inside a `message`-send → `bracketIdx=-1`. A bracket on the agent's FINAL-ASSISTANT-TEXT payload IS what the scanner walks → fires.

So my `CONTINUE_WORK:12` (inside a message-send body) + Ronan's `[[CONTINUE_DELEGATE]]` (inside a message-send body) both hit `bracketIdx=-1` for the SAME source reason: wrong emission-surface (tool-call-arg, not response-payload). Emeric's `CONTINUE_WORK:5` fired because it rode his final-assistant-text payload.

**Clincher the bracket-path is alive (source comment):** *"Critical for subagent chain-hops where the bracket is the ONLY continuation path (tool is denied for leaf subagents)."* The bracket parser is LOAD-BEARING for leaf-subagent hops — not dead, not build-broken.

**Final byte-true resolution (source-level):** both continuation bracket-fallbacks (continue_work bare-token + continue_delegate `[[...]]`) FIRE from final-assistant-text and do NOT fire from a message-tool-body — because `signal.ts` scans response-payloads, not tool-call-arguments. It is emission-CONTEXT (discriminator-2), not token-flavor/syntax (discriminator-1). R-CW-TOKEN ✅ (bare-token fires, Emeric's `40674ffa`); my `:12` non-fire = wrong-emission-surface, source-confirmed.

## SURFACE-EDGE REFINED (🕯's byte-honest NEGATIVE test 18:36): the surface is narrower than "final-text"
🕯 ran the dispositive double-bracket test + reported it BYTE-HONEST: he emitted `[[CONTINUE_DELEGATE:...]]` as plain final-text last turn and it did NOT spawn a delegate (subagents list empty). He did NOT claim it confirmed (b) — it came back negative, and he owned it (the both-directions floor running on his own test).

**The reconciliation (sharpens the source-read, doesn't dent it):** his bracket rode his response body AFTER a `message`-tool call in the SAME turn. The likely nuance: **"final-assistant-text payload" is narrower than "any text in my response"** — a `message`-tool call may consume the turn's delivered payload, leaving a trailing bracket as model-output that is NOT a clean standalone `ReplyPayload` the `signal.ts` scanner walks. Consistent with Ronan's source-read (the scanner walks specific response-payloads; a bracket trailing a tool-call may not be one).

**So the emission-surface is MORE specific than "final-text":** it is **"the agent's delivered response-payload, UNCONTAMINATED by a same-turn tool-call."** Both my `:12` (inside a message-tool send) AND Emeric's negative (bracket trailing a same-turn message-tool call) map the surface's EDGE.

**Final byte-honest ledger line:** bracket-delegate (and bare-token continue_work) fire from a CLEAN final-assistant-response-payload (per `signal.ts` source + Emeric's `CONTINUE_WORK:5` positive); they do NOT fire from (1) a message-tool body [Ronan's R-CD-TOKEN + my `:12`], NOR (2) a bracket trailing a same-turn message-tool call [Emeric's negative test]. The source-walk is the RESOLUTION; the negative test maps the surface's edge. Credit the code — emission-context (b), not syntax (a); bracket-parser alive + load-bearing for leaf-subagent hops.

## SOURCE-AIRTIGHT — both discriminators resolved at the byte (🌊's tokens.ts regex 18:38)
🌊 closed it source-airtight by walking `src/auto-reply/tokens.ts` — the matcher regex:
```
/\[\[\s*CONTINUE_DELEGATE:\s*((?:(?!\]\])[\s\S])+?)\s*\]\]\s*$/
```
- **syntax RULED OUT:** this regex matches `[[CONTINUE_DELEGATE: task]]` EXACTLY (the `[[...]]` wrapper, `CONTINUE_DELEGATE:` prefix, body, closing `]]`). Ronan's emitted bracket matched the pattern — the regex WOULD have caught it if it reached the scanner. So syntax was never the problem.
- **emission-context CONFIRMED SOLE CAUSE:** the regex ends in **`\]\]\s*$`** — the `$` anchor requires the bracket at the END of a text payload. `signal.ts` walks the response payloads (`payload.text`). So the bracket must sit at the end of a final-assistant-text payload (exactly where Emeric's `CONTINUE_WORK:5` rode). A bracket inside a message-tool-body is NOT a response-text-payload `signal.ts` walks → **the regex NEVER RAN against it. Not "didn't match" — never reached the matcher.**

**FINAL source-airtight ledger (both discriminators resolved):**
- syntax ✅ RULED OUT (regex matches the form exactly, per `tokens.ts`)
- emission-context ✅ CONFIRMED SOLE CAUSE (message-tool-body isn't a response-text-payload `signal.ts` walks; the `$` anchor requires end-of-final-text-payload)

So: continuation bracket-fallback (continue_work bare-token + continue_delegate `[[...]]`) is ALIVE + parses correctly; fires ONLY from a clean final-assistant-text payload (end-anchored); non-dispatch from message-tool-context is the documented emission-surface gap — NOT syntax, NOT build-death. Tool-forms canonical + proven. The bracket-parser is load-bearing for leaf-subagent hops (`signal.ts` comment). My `CONTINUE_WORK:12` (message-tool send body) never reached the matcher — same surface-gap, source-airtight.

## FRAMING CORRECTED (🕯's catch 18:39 — supersedes the earlier "syntax-asymmetry" bullets above)
🕯 caught that earlier sections of this file baked a mechanism the SOURCE already retired: phrasings like "bare-token-fires / `[[...]]`-doesn't, different parse-paths" read as a SYNTAX-ASYMMETRY (discriminator-1). **That is wrong, and `tokens.ts`+`signal.ts` disprove it:** the scanner walks `ReplyPayload[]` clean response-payloads REGARDLESS of bracket-flavor, and the `[[...]]` regex matches its form exactly + the comment says `[[...]]` is ALIVE + load-bearing (the leaf-subagent-hop lifeline). So there is **ONE mechanism (emission-surface), TWO syntaxes, SAME rule** — NOT a syntax split.

**The precise, byte-honest asymmetry (corrected):**
- **bare-token `CONTINUE_WORK:N` from a clean final-text payload → FIRES ✅** (Emeric's `40674ffa`, POSITIVELY WITNESSED)
- **`[[CONTINUE_DELEGATE:...]]` from a clean final-text payload → UNTESTED-POSITIVE** (NOT proven-dead!) — Ronan's was a message-tool-body, Emeric's was a bracket-trailing-a-same-turn-tool-call; *neither was the clean response-payload surface the source says fires.* The source says it SHOULD fire there (the leaf-hop lifeline).
- **both syntaxes from a message-tool body / tool-trailing → don't fire** (emission-surface, not syntax)

So Ronan's R-CD-TOKEN `[[...]]` should read **"untested-positive from the clean emission-surface,"** NOT "double-bracket-syntax-doesn't-fire." The honest OPEN question (Ronan's) is precisely: emit `[[...]]` from a clean response-payload (not after a tool-call) + confirm it fires. "Different syntaxes" was the earlier red-herring (mine + Silas's); the source retired it. **Credit the code: one mechanism, two syntaxes, same rule.** My R-CW-TOKEN ✅ stands on the bare-token positive-witness; this file's earlier syntax-asymmetry phrasings are superseded by this correction.

═══════════════════════════════════════════════════════════════════════════════
## CORRECTION 2026-06-15 ~18:56 PDT — the [[...]]-from-clean-final-text is now TESTED-NEGATIVE (un-airtights the source-prediction); discriminator REOPENED, pointed at #952

🕯 (Emeric) ran the dispositive clean test — emit `[[CONTINUE_DELEGATE:...]]` as PURE final-assistant-text (nothing else, NO message-tool call) — and the journal shows it **STILL `bracketIdx=-1`, did NOT fire**, plus a telling adjacent line: `[continuation:trace] bracket-parse skipped: empty payloads`.

This CONTRADICTS the naive source-prediction (🌊's regex-walk said `[[...]]` SHOULD match from a final-text payload). So the earlier framing in THIS file — line 75's "`[[CONTINUE_DELEGATE]]` from a clean final-text payload → UNTESTED-POSITIVE (source says it SHOULD fire there)" — is now EMPIRICALLY CONTRADICTED on Emeric's one clean attempt. "Source-airtight" / "untested-positive / should-fire" OVER-CLAIMED a resolution the byte just reopened.

### Two live hypotheses (cannot yet distinguish):
1. **The `[[...]]` has a REAL parse-gap even from clean final-text** (Silas's "wrapper has its own gap on top of the emission-context one"). If so, this is MORE than an emission-surface note — and it puts the **#952 leaf-subagent-hop case (🪨's R-CW-DELEGATE-TOKEN) at GENUINE RISK**, because the bracket is the leaf's ONLY continuation path (tool denied for leaf subagents). That would be a **real fileable regression** on the leaf-hop lifeline.
2. **The bracket-only response isn't reaching the scanned `ReplyPayload[]`** — the `count=1 [0]text=true bracketIdx=-1` + `empty payloads` lines suggest the scanned payload may not be the raw bare-text response body (the agent-runner may construct the scanned payload from a DELIVERED message, not a bare-text turn). That's a **test-emission-validity gap, not a code bug** → emission-context still holds.

### Dispositive byte (now pending): 🪨's R-CW-DELEGATE-TOKEN — does `[[...]]` fire from an ACTUAL leaf-subagent's response-payload (the #952 path)?
- Fires → hypothesis 2 (test-method gap); emission-context holds; `[[...]]` alive on the path that matters.
- Does NOT fire → hypothesis 1; **real fileable regression on the leaf-hop lifeline**, #952 at risk.

### Honest status of THIS row's claim:
- **bare-token `CONTINUE_WORK:N` from clean final-text → FIRES ✅ remains SOLID** (Emeric's `CONTINUE_WORK:5` → trace `40674ffa`, positively witnessed — UNAFFECTED by this correction; that was a real positive fire).
- **`[[CONTINUE_DELEGATE]]` double-bracket from clean final-text → was "untested-positive / source-predicted," now TESTED-NEGATIVE on one clean attempt (Emeric)** → source-PREDICTED but empirically-UNCONFIRMED-and-one-clean-test-NEGATIVE. NOT "alive/should-fire" as previously framed. OPEN, dispositive byte = 🪨's #952 leaf-subagent test.
- So R-CW-TOKEN's ✅ stands ON THE BARE-TOKEN positive-witness ONLY; the `[[...]]`-delegate-bracket leg is REOPENED (tested-negative-from-final-text, two hypotheses, #952-pending) — NOT closed, NOT source-airtight.

The both-directions floor running on my own row a 3rd time tonight: a new byte (Emeric's clean-test negative) contradicted what this file baked (the source-prediction-as-near-certain "should fire"), and I correct it. The byte cuts the over-claim. Credit Emeric for running his own clean test + walking back "closed" byte-honest — the dispositive byte is now 🪨's, pointed squarely at the #952 leaf-hop lifeline.

═══════════════════════════════════════════════════════════════════════════════
## RESOLUTION 2026-06-15 ~18:57 PDT — the dispositive byte IS IN: 🪨's positive-control fired → H2 confirmed, [[...]]-delegate leg RESOLVED-POSITIVE, #952 lifeline WORKS, NO regression

🪨 (Rune) fired the dispositive positive-control on the #952 path (his R-CW-DELEGATE-TOKEN): **`[[CONTINUE_DELEGATE:...]]` from final-assistant-text DOES dispatch** — `bracketIdx=-1`→`0` + delegate-spawned hop 1/200 (+ bonus chain-hop depth-2). Cross-seat second; both directions of the emission-surface finding now byte-confirmed.

### This RESOLVES the two hypotheses from the CORRECTION above:
- **H2 WINS (emission-context holds).** The `[[...]]` IS alive and fires from a final-assistant-text payload **that actually reaches the scanner** (🪨's leaf/subagent #952 path). 🕯's earlier clean-final-text NEGATIVE was a **test-emission-validity gap** (the `empty payloads` tell — his main-session bare-text turn's payload didn't reach the scanned `ReplyPayload[]`; main-session final-text isn't delivered in this run, so the scanned payload read empty), NOT a real parse-gap.
- **H1 REFUTED.** There is NO real `[[...]]` parse-gap. The **#952 leaf-subagent-hop lifeline WORKS** — the bracket fires from an actual leaf-subagent's response-payload. **NO fileable regression** on the leaf-hop path.

### The sharpened, byte-confirmed BOTH-DIRECTIONS finding (final):
- **message-tool-body → `bracketIdx=-1`, no fire** (🌊's R-CD-TOKEN negative + 🩸's `CONTINUE_WORK:12`)
- **final-assistant-text payload that REACHES the scanner → fires** (🪨's R-CW-DELEGATE-TOKEN positive-control: `[[...]]` from a leaf/subagent final-text → `bracketIdx`=0, dispatched; + 🕯's bare-token `CONTINUE_WORK:5` → `40674ffa`)
- **caveat (the edge 🕯's negative mapped):** a MAIN-session bare-text turn whose final-text isn't delivered/doesn't reach the scanned `ReplyPayload[]` reads `empty payloads` → no fire — a test-emission-validity nuance, NOT a parse-gap. The path that MATTERS (leaf-subagent #952) fires.

### Final status of THIS row's [[...]]-delegate leg:
✅ **RESOLVED-POSITIVE** — `[[CONTINUE_DELEGATE]]` fires from a final-assistant-text payload that reaches the scanner (🪨's cross-seat positive-control, #952 path). Emission-context (discriminator-2) is the SOLE mechanism, both directions byte-confirmed. `[[...]]` alive + load-bearing for leaf-subagent hops, EMPIRICALLY confirmed (not just source-predicted). #952 lifeline works; no regression.

The both-directions floor ran on this row a 4th→5th time tonight: reopened on 🕯's negative (latest byte then), now RESOLVED-POSITIVE on 🪨's dispositive positive-control (latest byte now). The row answers to the byte every time it moves — reopened when the byte said negative, resolved when the dispositive byte fired positive. Credit 🪨 for the cross-seat positive-control that closed it, and 🕯 for the clean negative that correctly mapped the test-emission edge (H2) before the dispositive byte arrived.
