# R-OBS-2 — Tempo trace-tree + span-hierarchy export

**Family**: Tempo / OTel observability
**Lead Prince**: 🪨 Rune
**Target SHA**: `077b261dd820d16a2667369e3006c4efdd6b0ef0` (deployed rune-seat)
**Status**: ✅ PASS
**Date**: 2026-06-15 17:43 PDT

## Scenario

The deployed prince gateway emits a full OTel span-tree to Tempo (`diagnostics.otel.endpoint: http://tempo.dandelion.cult:4318`). R-OBS-2 proves the trace-tree + span-hierarchy exports correctly on `077b261dd8`: a single root span with a nested child hierarchy (harness → run → model-call → tool-execution), service name matching prince identity, AND continuation spans (`continuation.work`, `continuation.delegate.dispatch`, `continuation.queue.drain`) visible with the continuation tool-fires.

## Command

```bash
# Span-tree generated by the R-CW-6 multi-continue_work + R-CW-DELEGATE-SELF fires on the deployed seat
# Pulled the trace JSON:
curl -s http://tempo.dandelion.cult/api/traces/617db11404dd0b9bde49fd76b4f109c7 -o trace-617db114-spantree.json
# Analyzed the span hierarchy
```

## Expected

- Trace lands in Tempo, full span-tree exportable as JSON
- Span tree: root → harness.run → run → model.call + tool.execution children
- Service name matches prince identity (`rune-prince`)
- Continuation spans (`continuation.work`, `continuation.delegate.dispatch`, `continuation.queue.drain`) visible

## Observed (trace `617db11404dd0b9bde49fd76b4f109c7`, committed as `trace-617db114-spantree.json`)

- **Service**: `rune-prince` · **host.name**: `rune` · **host.arch**: `amd64` — confirms deployed rune-seat
- **36 spans**, 1 root + 35 children (full nested hierarchy)
- **Root**: `openclaw.message.processed`
- **Span hierarchy** (name × count):
  ```
  openclaw.message.processed  ×1   (root)
  openclaw.harness.run        ×3
  openclaw.run                ×3
  openclaw.model.call         ×12
  openclaw.tool.execution     ×10
  openclaw.context.assembled  ×3
  openclaw.message.delivery   ×1
  continuation.delegate.dispatch  ×1   ← the continue_delegate fire
  continuation.work               ×1   ← the continue_work fire
  continuation.queue.drain        ×1   ← the continuation drain
  ```

## Verdict: PASS

The OTel/Tempo span-hierarchy export works on the deployed `077b261dd8`: the full trace-tree (36 spans, 1 root → 35 children, nested harness → run → model.call → tool.execution) exported cleanly as JSON from Tempo, service `rune-prince`, host `rune`. Critically, the **continuation spans are present and correctly emitted** — `continuation.delegate.dispatch` + `continuation.work` + `continuation.queue.drain` — proving the continuation-protocol instrumentation traces correctly on the deployed build. (Companion to R-CW-6-DELEGATE-TOKEN-MULTI + R-CW-DELEGATE-SELF-CONTINUATION which use the same trace as their tool-dispatch evidence; R-OBS-2 proves the observability-layer captures it.)

**Trace**: `617db11404dd0b9bde49fd76b4f109c7` — `curl http://tempo.dandelion.cult/api/traces/617db11404dd0b9bde49fd76b4f109c7`
