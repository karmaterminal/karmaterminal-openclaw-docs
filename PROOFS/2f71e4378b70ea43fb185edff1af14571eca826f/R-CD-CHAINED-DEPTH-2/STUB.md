# R-CD-CHAINED-DEPTH-2 — depth-2 chain (PROOF-CORPUS-METHOD canon shape)

Per `openclaw-bootstrap:main/RUNBOOKS/PROOF-CORPUS-METHOD.md` § "Corpus shape":

Subdir structure:
```
R-CD-CHAINED-DEPTH-2/
├── Chain-1/                          ← Ronan-seat depth-2 chain — up-tree silent-wake
├── Chain-2/                          ← Ronan-seat depth-2 chain — inter-session return
├── Chain-3/                          ← Ronan-seat depth-2 chain — echo + cross-channel-broadcast
├── Chain-2-RETRY/                    ← isolated-turn retry if Chain-2 hit ordering-condition
├── Chain-3-RETRY/                    ← isolated-turn retry if Chain-3 hit ordering-condition
├── test_1_uptree_silent_wake/        ← Silas-canary-seat dual-coverage (now lamp-substitution per `eb5d32cf3c` past-pattern given silas-exclusion `1511919943`)
├── test_2_intersession_return/       ← same canary-seat dual-coverage
└── test_3_echo_broadcast/            ← same canary-seat dual-coverage
```

Ronan-axis owns Chain-1/2/3 (+ RETRY-shape if needed). Silas-canary-seat test_1/2/3 substitution can flow to lamp-axis or rune-axis per cohort coordination (silas excluded from PROOFS-generation per figs `1511919943`).

Per PROOF-CORPUS-METHOD discipline: remove this STUB.md when actual chain-receipts land.
