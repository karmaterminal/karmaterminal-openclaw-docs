# Clawsweeper entrypoint

Read `proofs-manifest.json`, verify exact pure SHA
`2ffc7ca0615f5917acf809d1ccba82b0ef5b2d5a`, then inspect each row's
`EVIDENCE.md`. Treat execution composite `37300f29…` as historical
ancestry-bound live evidence, never as the exact target. Exact pure gates and
red Mode-B are under `artifacts/gates/`; live proof on descendant runtime
`a48c475b…` is explicitly pending.
