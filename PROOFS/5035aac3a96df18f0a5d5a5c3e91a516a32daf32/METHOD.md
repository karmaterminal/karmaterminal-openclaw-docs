# Method

Execute each required row once under the accepted harness and row-specific environment. Preserve semantic failures. Retry only classified infrastructure failures with new attempt identity and retained originals. Live rows may use the exact deployed runtime composite, but canonical corpus identity remains the pure continuation SHA.
