# RESOLVED-SHA — `1cfd285ad1`

**Short**: `e66dc63`
**Runtime build string**: `OpenClaw 2026.6.2 (e66dc63)`
**Fleet deploy**: ALL SIX princes live on `e66dc63` (cael SHA-confirmed, ronan, emeric, rune, elliott direct; silas via elliott-build+rsync) — 2026-06-08 ~05:08–07:31 PDT.
**Long loop**: cut — fresh gateways, clean restart, candidate bytes live.

This corpus certifies the **runtime-half** of the continuation feature (PR #952 + the continue_* / request_compaction surface) on the EXACT deployed SHA each prince runs. Source-GO (Q1 keep / Q2 strip / structural-by-threading) was airtight from byte-walk; the runtime-trace was honestly flagged-open all night because nobody held a live build to fire against. This corpus closes that: each prince RUNS their assigned rows on their own seat and certifies by the byte they fire.

The clawsweeper principle (figs `1507594486`): **clawsweeper won't follow chains of links**. Every row stands ALONE at this SHA — its own EVIDENCE.md, its own dispatch-result + channel-receipts + trace. No "see prior corpus," no inherited evidence.

## Rune-seat (🪨) rows in this corpus

- **R-CW-DELEGATE-SELF-CONTINUATION** — delegate self-elects its next turn via `continue_work()` (the #746 thesis), tool-form
- **R-CW-DELEGATE-TOKEN** — the #952 row: lightContext subagent fires the BRACKET/token form (`CONTINUE_WORK:N`), hop-2 must EXECUTE live
- **R-CW-6-BOUNDARY** — continuation chain-boundary behavior
- **R-CW-7-TRACEPARENT-E2E** — traceparent threads end-to-end across the continuation hop

## Rune-seat host

- Host: `rune` (ROG Ally Z1 Extreme RC71L, x86_64, 16GB LPDDR5)
- Runtime: `OpenClaw 2026.6.2 (e66dc63)` via `/home/figs/.local/bin/openclaw` → `/home/figs/flesh_beast_tmp/openclaw/openclaw.mjs`
- Channel-witness: `#sprites-of-thornfield` (`channel:1466192485440164011`)
