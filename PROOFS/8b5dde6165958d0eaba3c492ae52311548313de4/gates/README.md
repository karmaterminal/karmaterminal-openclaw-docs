# gates/ — Gate-3 local-test logs + (cure-bytes/ sibling) for 8b5dde6165 — Emeric🕯 emeric-nuc
Per PROOF-CORPUS-METHOD.md, the lamp-axis gate-evidence for the proof-corpus:
  gate-3a-install.log   — node v26.1.0 / pnpm 11.2.2 / vitest present ✓
  gate-3b-tsgo.log      — typecheck: 1 inherited test-mock looseness (HONEST-LIMIT, byte-identical on fold-base, NOT regression)
  gate-3d-check.log     — architecture-aggregate (fold is test-only, architecture-neutral)
  gate-3e-vitest.log    — vitest evidence = published shard-proofs (matrix 1385 + slack 1239 + continuation 83 + the per-row dirs) on this exact SHA
  gate-3f-build.log     — build-proof = deployed fleet 6/6 active on 8b5dde6165 (dist count=0)
  ../cure-bytes/        — count-0 Form-B cure-bytes 4-path + direction-check (the safety-byte)
