# Frozen-basis c713 Mode-B packet

Every `c7131791...` occurrence under this directory is immutable frozen-basis
qualification for run `32911065508`, workflow
`342cc9c6d190e1ba57d9995d29e394c993a3e79b`. The aggregate and three red shard
packets are preserved byte-for-byte. They are not exact warm-target Mode-B or
execution.

Vendored scope:

- `aggregate/`: fail-closed aggregate summary, job timings, and routing receipts.
- `artifacts/...dist-core-runtime-tui-pty/`: 13 deterministic missing-dist
  failures plus one greened load flake.
- `artifacts/...node-agentic-commands-doctor/`: one deterministic hosted timeout.
- `artifacts/...node-agentic-cli/`: four deterministic runner-order failures.
- `BLOB-IDENTITY.tsv`: exact source/frozen/upstream blob comparison.
