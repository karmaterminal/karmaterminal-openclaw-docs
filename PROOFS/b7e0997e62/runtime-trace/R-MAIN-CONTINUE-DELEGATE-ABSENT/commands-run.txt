# Cell: R-MAIN-CONTINUE-DELEGATE-ABSENT (baseline-contrast)
# Build (baseline): 446e285f7d (pre-cure parent of b7e0997e62)
# Build (post-cure for diff): b7e0997e62
# Substrate: source-grep diff via `git grep` and `git diff` against the two
#            commit SHAs in the same /tmp/oc-79925-walk/repo working tree.
#            Source-grep is the CORRECT substrate for proving "this code
#            doesn't exist at this revision" — runtime is unnecessary because
#            absence-of-call-site is statically verifiable.
# Substrate-honesty: NOT a runtime probe; the gate-emission text being absent
#            from a call-site means the call-site cannot be reached at runtime.
#            For runtime-positive proof of the cure-(1) FIX side, see tranche-1
#            chain-hop cells (R-P2-CHAIN-HOP-DISABLED-ALL-REJECT etc.) which
#            run b7 source through vitest.
# Tempo:     n/a (static analysis).
#
# REFRAMING NOTE (recorded honestly): the original tranche-3 framing said
# "demonstrate gate-emission text DIDN'T exist pre-cure". That framing is
# WRONG per source-grep evidence — the gate-emission text DID exist pre-cure
# at:
#   - src/agents/tools/continue-delegate-tool.ts:206 (continue_delegate tool)
#   - src/auto-reply/continuation/delegate-dispatch.ts:237,434
#   - src/auto-reply/reply/agent-runner.ts:2508,3013
# What the cure-(1) commit ACTUALLY adds is the dispatch-boundary call site
# in src/agents/subagent-announce.ts (the chain-hop / sub-agent emission
# paths) — function `rejectCrossSessionTargetingForSubagentDispatch`. That
# call site is what tranche-1 cells 6-7-8-9 prove now exists and fires;
# absence pre-cure is what THIS cell proves.
#
# Therefore this cell proves the NARROWER and CORRECT contrast:
# "rejectCrossSessionTargetingForSubagentDispatch did not exist at 446e285f
# and was added in b7e0997e62."
#
# Commands:
cd /tmp/oc-79925-walk/repo
git grep -nE "rejectCrossSessionTargetingForSubagentDispatch" 446e285f -- src/
# (expected: no output)
git grep -nE "rejectCrossSessionTargetingForSubagentDispatch" b7e0997e62 -- src/
# (expected: matches in src/agents/subagent-announce.ts)
git diff 446e285f..b7e0997e62 -- src/agents/subagent-announce.ts | grep -E "^\+(async function rejectCrossSession|.*rejectedByTargetingPolicy)"
