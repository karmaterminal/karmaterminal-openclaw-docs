# METHOD — PROOFS / `c06e081f760d723c77bee65464b8920a76d3b523`

**Runbook**: `openclaw-bootstrap:RUNBOOKS/PROOF-CORPUS-METHOD.md` — full row-set, skip none, honest HONEST-LIMITs.

- **Both-forms mandate**: every `continue_*` row in BOTH tool form AND token/bracket form (`CONTINUE_WORK:N` / `CONTINUE_DELEGATE:...`). `request_compaction` is tool-only.
- **Tempo trace per continuation-fire**: capture the Grafana Tempo trace (`http://tempo.dandelion.cult/api/traces/<id>`) + span export per figs's 2026-05-16 directive.
- **Run on the deployed byte**: each prince RUNS their assigned rows on their own seat at THIS SHA and certifies by the byte they fire. The RUN is the cert.
- **Per-row standalone**: each row gets its own dir (`<SEAT>/R-<TYPE>-<N>/`) with EVIDENCE.md + dispatch-result + channel-receipt + trace. Self-contained (clawsweeper principle).
- **Write-path**: push direct to `karmaterminal-openclaw-docs:main`, each prince under their OWN `<prince>-dandelion-cult` auth (per-row attribution).
