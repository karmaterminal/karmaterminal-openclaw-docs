{
  "schema": "openclaw.k6.proof-row-manifest.v1",
  "rowId": "R-CONFIG-INTERSESSION",
  "issue": 121,
  "candidateSha": "${OPENCLAW_CANDIDATE_SHA}",
  "seat": "${OPENCLAW_SEAT_NAME:-cael-dgx}",
  "sessionKey": "${OPENCLAW_SESSION_KEY:-main}",
  "transport": "websocket",
  "toolSurface": "read-only",
  "mutates": false,
  "timeoutSeconds": 60,
  "scenario": {
    "name": "r-config-intersession",
    "description": "Read-only continuation cross-session targeting config receipt via direct authenticated operator config.get RPC.",
    "methods": [
      "config.get"
    ],
    "status": "runnable",
    "file": "r-config-intersession.js"
  },
  "expectedReceipts": [
    {
      "name": "config-read",
      "required": true,
      "description": "Configuration read successful"
    },
    {
      "name": "cross-session-targeting",
      "required": true,
      "description": "agents.defaults.continuation.crossSessionTargeting explicitly observed as enabled"
    }
  ],
  "artifactDestination": {
    "root": "PROOFS",
    "sha": "${OPENCLAW_CANDIDATE_SHA}",
    "row": "R-CONFIG-INTERSESSION",
    "seat": "${OPENCLAW_SEAT_NAME:-cael-dgx}",
    "runDirPrefix": "manual-corpus"
  },
  "review": {
    "candidateOnly": true,
    "foldRequiresReview": true,
    "notes": "Read-only direct operator-RPC config.get row. Projects only the explicit crossSessionTargeting=enabled value; no agent-mediated owner-only gateway tool, model sentinel, config mutation, or continuation scheduling."
  },
  "liveRunSafety": {
    "classification": "k6-runnable",
    "requiresLiveGatewayToken": true,
    "requiresTargetSessionKey": false,
    "requiresCandidateSha": true,
    "requiresExternalAgentOrToolInvocation": false,
    "requiresHumanConfirmation": false,
    "sameSessionConcurrencySafe": true,
    "expectedArtifactClass": "PASS-candidate",
    "requiredReceipts": [
      "seat-readiness",
      "config-read",
      "cross-session-targeting"
    ],
    "foldRequiresReview": true,
    "notes": "Read-only direct operator-RPC config.get; public evidence must project only required continuation fields plus a public-safe query-window correlation receipt. No agent-mediated owner-only tool or model sentinel."
  },
  "gateway": {
    "wsEnv": "OPENCLAW_GATEWAY_WS",
    "tokenEnv": "OPENCLAW_GATEWAY_TOKEN"
  }
}
