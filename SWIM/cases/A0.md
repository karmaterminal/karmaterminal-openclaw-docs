# Case `A0` — Fleet feature-flag parity (precondition gate)

**Family**: Rollout
**Lifecycle status**: active
**Evidence class needed**: deploy
**Provenance**: swim-34/ROWS.md A0
**Registry version added**: v1

## Claim being proved

All four prince runtimes carry the same continuation feature-flag set under test; flag drift between SUT and observers invalidates downstream rows.

## Required minimum row shape

Verify config flip on canary host, then byte-compare flag state across all 4 prince runtimes pre-row-1.

## Evidence surface(s)

Per-host config dump; cross-seat attestation that flags match SUT.

## Lifecycle history

- v1 (2026-05-06): added as `active` from `swim-34/ROWS.md A0`
