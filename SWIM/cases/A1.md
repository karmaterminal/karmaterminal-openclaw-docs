# Case `A1` — Flow registry + per-agent sessions persistence across restart

**Family**: Turns
**Lifecycle status**: active
**Evidence class needed**: live-row,cross-seat
**Provenance**: swim-34/ROWS.md A1
**Registry version added**: v1

## Claim being proved

TaskFlow flow_runs table and per-agent session jsonl files survive gateway restart with no loss of in-flight continuation state.

## Required minimum row shape

Stage flows + sessions, restart gateway, verify both surfaces present and consistent post-restart on driver AND SUT.

## Evidence surface(s)

flow_runs sqlite snapshot pre/post-restart; per-agent jsonl diff; cross-seat byte-pin from SUT.

## Lifecycle history

- v1 (2026-05-06): added as `active` from `swim-34/ROWS.md A1`
