# Case `B2` — F2 noisy continue_work (inbound during reservation)

**Family**: Turns
**Lifecycle status**: active
**Evidence class needed**: live-row
**Provenance**: swim-34/ROWS.md B2; F-series
**Registry version added**: v1

## Claim being proved

Self-elected continue_work() handles inbound message arriving during the reservation window correctly: either fold-into-current-turn or honor reservation, but never both.

## Required minimum row shape

Call delayed continue_work(), inject inbound message during delay window, verify exactly-once turn execution with declared semantics.

## Evidence surface(s)

Inbound timestamp; turn-start timestamp; result-vs-cancellation tie-break.

## Lifecycle history

- v1 (2026-05-06): added as `active` from `swim-34/ROWS.md B2; F-series`
