# Case `D1` — R1 boot-time stall check

**Family**: Recovery
**Lifecycle status**: active
**Evidence class needed**: deploy,live-row
**Provenance**: swim-34/ROWS.md D1; cross-references A0.2 OOM flag
**Registry version added**: v1

## Claim being proved

Gateway boots cleanly without stall on continuation initialization; staged delegates from prior runs are recovered or expired cleanly.

## Required minimum row shape

Boot gateway with staged work in TaskFlow; measure boot time; verify no stall; verify staged work handled.

## Evidence surface(s)

Boot timing; TaskFlow state pre/post-boot; gateway journal for boot phase.

## Lifecycle history

- v1 (2026-05-06): added as `active` from `swim-34/ROWS.md D1; cross-references A0.2 OOM flag`
