# Case `N003` — fanoutMode `tree` return (resolved-ancestor recipient set)

**Family**: Routes
**Lifecycle status**: new
**Evidence class needed**: live-row, cross-seat
**Provenance**: RFC §6.8 fanout-mode contract; PR #551
**Registry version added**: v1

## Claim being proved

continue_delegate with `fanoutMode: "tree"` resolves the recipient set to the ancestor-keys of the dispatching session, and each ancestor receives identical `traceparent`. Spawn-as-link semantic preserved across the fanout.

## Required minimum row shape

Build a multi-hop continuation chain so the dispatching session has known ancestors. Fire delegate with `fanoutMode: "tree"`. Verify each ancestor seat receives the return payload with shared `traceparent`. Verify recipient set matches the resolved ancestor list (no extras, no missing).

## Evidence surface(s)

Pre-fanout ancestor-key snapshot; per-ancestor session jsonl trajectories; trace-tree query confirming shared `traceparent` across the resolved set.

## Lifecycle history

- v1 (2026-05-06): added as `new` from RFC §6.8 fanout contract
