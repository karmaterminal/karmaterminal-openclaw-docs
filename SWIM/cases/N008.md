# Case `N008` — request_compaction success path with cooldown arming + diagnostic count

**Family**: Recovery
**Lifecycle status**: new
**Evidence class needed**: live-row
**Provenance**: RFC §4.2 volitional-compaction contract; `src/agents/tools/request-compaction-tool.ts`
**Registry version added**: v1

## Claim being proved

Volitional compaction uses the active session provider, model, and auth context. If background compaction resolves as `{ ok: true, compacted: true }`, the per-session cooldown is armed AND the diagnostic `volitional` counter increments.

## Required minimum row shape

Fire request_compaction with valid context. Verify background compaction resolves successful. Verify per-session cooldown armed (subsequent same-session request blocked or deferred per cooldown). Verify diagnostic counter incremented.

## Evidence surface(s)

Tool call result; cooldown state pre/post; diagnostic counter pre/post.

## Lifecycle history

- v1 (2026-05-06): added as `new` from RFC §4.2 volitional-compaction contract
