# Case `X1` — Public continuation tool visibility matrix (across session kinds)

**Family**: Observability
**Lifecycle status**: active
**Evidence class needed**: repo-test,live-row
**Provenance**: #412 X1; SUT-VERIFIED via P1 regression REGISTRATION-path trace
**Registry version added**: v1

## Claim being proved

Public continuation tools (continue_work, continue_delegate, request_compaction) are visible exactly where the contract specifies, across all session kinds (main / delegate / leaf / heartbeat / etc.).

## Required minimum row shape

Enumerate session-kind × tool matrix; verify visibility matches contract for every cell.

## Evidence surface(s)

Tool-visibility query per session kind; contract-vs-actual diff.

## Lifecycle history

- v1 (2026-05-06): added as `active` from `#412 X1; SUT-VERIFIED via P1 regression REGISTRATION-path trace`
