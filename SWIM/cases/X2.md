# Case `X2` — Main-session vs delegate vs leaf tool visibility

**Family**: Observability
**Lifecycle status**: active
**Evidence class needed**: repo-test,live-row
**Provenance**: #412 X2; same family as X1
**Registry version added**: v1

## Claim being proved

Tool visibility differs correctly across main-session / delegate / leaf contexts; no leak of restricted tools to wrong context.

## Required minimum row shape

Verify each tool's visibility per context level; verify no over-leak or under-leak.

## Evidence surface(s)

Per-context tool-list snapshot.

## Lifecycle history

- v1 (2026-05-06): added as `active` from `#412 X2; same family as X1`
