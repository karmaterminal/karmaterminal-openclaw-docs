# Case `X5` — request_compaction guard / reachability / threshold behavior

**Family**: Recovery
**Lifecycle status**: active
**Evidence class needed**: live-row
**Provenance**: #412 X5
**Registry version added**: v1

## Claim being proved

request_compaction tool is reachable when guards permit, blocked when guards deny, and threshold-aware per declared semantics.

## Required minimum row shape

Test reachability under each guard state; test threshold behavior at boundaries.

## Evidence surface(s)

Tool result per guard state; threshold response evidence.

## Lifecycle history

- v1 (2026-05-06): added as `active` from `#412 X5`
