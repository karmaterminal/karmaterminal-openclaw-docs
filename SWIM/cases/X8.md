# Case `X8` — Max delegates per turn — boundary exploration

**Family**: Guards
**Lifecycle status**: active
**Evidence class needed**: live-row
**Provenance**: #412 X8
**Registry version added**: v1

## Claim being proved

Per-turn delegate cap is enforced live: exceeding it produces clean rejection of the excess, not silent drop of the limit.

## Required minimum row shape

Fire cap+1 delegates in single turn; verify rejection of excess.

## Evidence surface(s)

Delegate-fire count per turn; rejection response.

## Lifecycle history

- v1 (2026-05-06): added as `active` from `#412 X8`
