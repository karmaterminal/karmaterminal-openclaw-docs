{
  "schema": "openclaw.k6.proof-row-manifest.v1",
  "rowId": "PREFLIGHT",
  "issue": 101,
  "candidateSha": "${OPENCLAW_CANDIDATE_SHA}",
  "seat": "${OPENCLAW_SEAT_NAME:-ci-runner}",
  "sessionKey": "${OPENCLAW_SESSION_KEY}",
  "transport": "websocket",
  "toolSurface": "read-only",
  "mutates": false,
  "timeoutSeconds": 30,
  "gateway": {
    "wsEnv": "OPENCLAW_GATEWAY_WS",
    "tokenEnv": "OPENCLAW_GATEWAY_TOKEN"
  },
  "scenario": {
    "name": "preflight",
    "description": "Authenticate and resolve a model-selected session from sessions.list, or explicitly create and verify a disposable session, before a read-only exact-session tools.effective probe completes preparation authority.",
    "methods": [
      "health",
      "status",
      "agents.list",
      "models.list",
      "sessions.list",
      "sessions.create",
      "tools.effective"
    ],
    "status": "runnable",
    "file": "preflight.js"
  },
  "liveRunSafety": {
    "classification": "k6-runnable",
    "requiresLiveGatewayToken": true,
    "requiresTargetSessionKey": false,
    "requiresCandidateSha": false,
    "requiresExternalAgentOrToolInvocation": false,
    "sameSessionConcurrencySafe": true,
    "expectedArtifactClass": "PASS-candidate",
    "requiredReceipts": [
      "manifest-loaded",
      "k6-summary",
      "seat-readiness",
      "session-preparation-authority"
    ],
    "foldRequiresReview": true,
    "notes": "The default live path validates a session returned by sessions.list and remains read-only. OPENCLAW_PREFLIGHT_CREATE_DISPOSABLE_SESSION=true selects the explicit create-and-verify path. Metadata alone never marks preparation complete; only a successful exact-session tools.effective probe does. Session-resolution or inventory failures remain NO-VERDICT."
  },
  "expectedReceipts": [
    {
      "name": "manifest-loaded",
      "required": true,
      "pathHint": "row-manifest.json",
      "description": "The exact row manifest used for the k6 run."
    },
    {
      "name": "k6-summary",
      "required": true,
      "pathHint": "k6-summary.json",
      "description": "Raw k6 summary/export for the run."
    },
    {
      "name": "seat-readiness",
      "required": true,
      "pathHint": "seat-readiness.json",
      "description": "Public-safe seat/tooling preflight: k6 path/version, gateway reachability, candidate SHA validity, env presence booleans, seat/session class, and concurrency safety."
    },
    {
      "name": "tool-inventory",
      "required": false,
      "pathHint": "artifacts/tool-inventory.json",
      "description": "Live preflight only: tools.effective inventory for the target session."
    },
    {
      "name": "session-inventory",
      "required": false,
      "pathHint": "artifacts/session-inventory.json",
      "description": "Live preflight only: sessions.list/session resolution receipt."
    },
    {
      "name": "session-preparation-authority",
      "required": true,
      "pathHint": "runner-metadata.json",
      "description": "Public-safe listed/disposable session class plus product/runtime/harness identity, agent, model, runtime, explicit preparation stage, and tools.effective call stage; raw session keys are excluded."
    }
  ],
  "artifactDestination": {
    "root": "PROOFS",
    "sha": "${OPENCLAW_CANDIDATE_SHA}",
    "row": "PREFLIGHT",
    "seat": "${OPENCLAW_SEAT_NAME:-ci-runner}",
    "runDirPrefix": "k6-run"
  },
  "review": {
    "candidateOnly": true,
    "foldRequiresReview": true,
    "notes": "Preflight should never write a product verdict by itself. Session/model preparation and gateway readiness failures are setup NO-VERDICT outcomes."
  }
}
