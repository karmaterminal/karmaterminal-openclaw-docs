{
  "schema": "openclaw.k6.proof-row-manifest.v1",
  "rowId": "R-CW-MULTI-COLLAPSE",
  "issue": 481,
  "candidateSha": "${OPENCLAW_CANDIDATE_SHA}",
  "seat": "${OPENCLAW_SEAT_NAME:-cael-dgx}",
  "sessionKey": "${OPENCLAW_SESSION_KEY:-main}",
  "transport": "process-local",
  "toolSurface": "typed-tool",
  "mutates": false,
  "timeoutSeconds": 120,
  "scenario": {
    "name": "r-cw-multi-collapse-process-local",
    "description": "Exact-product process-local fixture for newest, within-grace, stale queued, and running collapse boundaries.",
    "methods": [
      "partitionSupersededWork",
      "exact-candidate-vitest"
    ],
    "status": "scaffold",
    "expectedFile": "run-multi-collapse-fixture.mjs",
    "registryNotes": "Executed explicitly by the producer catalog; never by k6."
  },
  "invocation": {
    "fixture": "tools/k6-proofs/scripts/run-multi-collapse-fixture.mjs",
    "graceMultiplierSymbol": "SUPERSEDED_GRACE_MULTIPLIER",
    "graceMs": "maxDelayMs * SUPERSEDED_GRACE_MULTIPLIER"
  },
  "expectedReceipts": [
    {
      "name": "newest-drives",
      "required": true,
      "description": "Newest election always drives."
    },
    {
      "name": "within-grace-older-drives",
      "required": true,
      "description": "Older election at the equality/within-grace boundary still drives."
    },
    {
      "name": "stale-queued-older-folds",
      "required": true,
      "description": "Only an older queued election strictly beyond grace folds."
    },
    {
      "name": "running-never-folds",
      "required": true,
      "description": "Running work never folds, even when stale and older."
    },
    {
      "name": "baseline-restored",
      "required": true,
      "description": "Exact product SHA/tree/status is unchanged after the fixture."
    }
  ],
  "artifactDestination": {
    "root": "PROOFS",
    "sha": "${OPENCLAW_CANDIDATE_SHA}",
    "row": "R-CW-MULTI-COLLAPSE",
    "seat": "${OPENCLAW_SEAT_NAME:-cael-dgx}",
    "runDirPrefix": "manual-corpus"
  },
  "review": {
    "candidateOnly": true,
    "foldRequiresReview": true,
    "notes": "Process-local exact-candidate producer. The fixture derives grace from maxDelayMs * SUPERSEDED_GRACE_MULTIPLIER and runs the real product tests."
  },
  "liveRunSafety": {
    "classification": "orchestration-required",
    "requiresLiveGatewayToken": false,
    "requiresTargetSessionKey": false,
    "requiresCandidateSha": true,
    "requiresExternalAgentOrToolInvocation": false,
    "requiresHumanConfirmation": false,
    "sameSessionConcurrencySafe": true,
    "expectedArtifactClass": "PASS-candidate",
    "requiredReceipts": [
      "newest-drives",
      "within-grace-older-drives",
      "stale-queued-older-folds",
      "running-never-folds",
      "baseline-restored"
    ],
    "foldRequiresReview": true,
    "notes": "The whole-set producer runner executes this fixture explicitly. Static corpus validation cannot satisfy it."
  }
}
