{
  "schema": "openclaw.k6.proof-row-manifest.v1",
  "rowId": "R-CW-MULTI-COLLAPSE",
  "issue": 481,
  "candidateSha": "${OPENCLAW_CANDIDATE_SHA}",
  "seat": "${OPENCLAW_SEAT_NAME:-cael-dgx}",
  "sessionKey": "${OPENCLAW_SESSION_KEY:-main}",
  "transport": "offline",
  "toolSurface": "typed-tool",
  "mutates": false,
  "timeoutSeconds": 90,
  "scenario": {
    "name": "r-cw-multi-collapse-producer",
    "description": "Process-local producer fixture: newest drives, within-grace older drives, only stale queued older folds, running never folds. graceMs=maxDelayMs*SUPERSEDED_GRACE_MULTIPLIER.",
    "methods": [
      "partitionSupersededWork"
    ],
    "status": "scaffold",
    "expectedFile": "r-cw-multi-collapse-producer.js",
    "registryNotes": "Use tools/k6-proofs/scripts/run-multi-collapse-fixture.mjs against the exact product checkout. Not a live gateway fire and not the static corpus validator."
  },
  "invocation": {
    "fixture": "tools/k6-proofs/scripts/run-multi-collapse-fixture.mjs",
    "graceMultiplierSymbol": "SUPERSEDED_GRACE_MULTIPLIER",
    "graceMs": "maxDelayMs*SUPERSEDED_GRACE_MULTIPLIER"
  },
  "expectedReceipts": [
    {
      "name": "newest-drives",
      "required": true,
      "description": "Newest election drives"
    },
    {
      "name": "within-grace-older-drives",
      "required": true,
      "description": "Within-grace older also drives"
    },
    {
      "name": "stale-queued-older-folds",
      "required": true,
      "description": "Only stale queued older folds"
    },
    {
      "name": "running-never-folds",
      "required": true,
      "description": "Running row never folds"
    },
    {
      "name": "baseline-restored",
      "required": true,
      "description": "Product worktree/baseline restored after fixture"
    }
  ],
  "artifactDestination": {
    "root": "PROOFS",
    "sha": "${OPENCLAW_CANDIDATE_SHA}",
    "row": "R-CW-MULTI-COLLAPSE",
    "seat": "${OPENCLAW_SEAT_NAME:-cael-dgx}",
    "runDirPrefix": "k6-run"
  },
  "review": {
    "candidateOnly": true,
    "foldRequiresReview": true,
    "notes": "Process-local collapse producer. Do not conflate with R-CW-MULTI staggered wakes. Historical PASS packets are fixtures only."
  },
  "liveRunSafety": {
    "classification": "orchestration-required",
    "requiresLiveGatewayToken": false,
    "requiresTargetSessionKey": false,
    "requiresCandidateSha": true,
    "requiresExternalAgentOrToolInvocation": false,
    "sameSessionConcurrencySafe": true,
    "expectedArtifactClass": "PASS-candidate",
    "requiredReceipts": [
      "newest-drives",
      "within-grace-older-drives",
      "stale-queued-older-folds",
      "running-never-folds",
      "baseline-restored"
    ],
    "foldRequiresReview": true,
    "notes": "Exact-candidate isolated fixture. Static validator is not this producer."
  }
}
