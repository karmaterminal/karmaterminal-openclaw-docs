{
  "schema": "openclaw.k6.proof-row-manifest.v1",
  "rowId": "R-RC-1",
  "issue": 104,
  "candidateSha": "${OPENCLAW_CANDIDATE_SHA}",
  "seat": "${OPENCLAW_SEAT_NAME:-cael-dgx}",
  "sessionKey": "${OPENCLAW_SESSION_KEY:-r-rc-reject}",
  "transport": "websocket",
  "toolSurface": "typed-tool",
  "mutates": false,
  "timeoutSeconds": 60,
  "scenario": {
    "name": "r-rc-1-threshold-reject",
    "description": "request_compaction below-threshold structured reject via disposable session/inventory-only path",
    "methods": [
      "sessions.create",
      "tools.effective",
      "sessions.messages.subscribe",
      "sessions.send"
    ],
    "status": "runnable",
    "file": "r-rc-1-threshold-reject.js"
  },
  "invocation": {
    "tool": "request_compaction"
  },
  "expectedReceipts": [
    {
      "name": "tool-invoke-rejected",
      "required": true,
      "description": "Authoritative request_compaction toolResult returned status=rejected and guard=context_threshold"
    },
    {
      "name": "tempo-trace-json",
      "required": true,
      "description": "Unique public-safe request_compaction tool trace recovered by seat, tool name, and the bounded row window"
    },
    {
      "name": "tool-trace-correlation",
      "required": true,
      "description": "Deterministic receipt pins the exact request_compaction tool span and its non-error status"
    }
  ],
  "artifactDestination": {
    "root": "PROOFS",
    "sha": "${OPENCLAW_CANDIDATE_SHA}",
    "row": "R-RC-1",
    "seat": "${OPENCLAW_SEAT_NAME:-cael-dgx}",
    "runDirPrefix": "k6-run"
  },
  "review": {
    "candidateOnly": true,
    "foldRequiresReview": true,
    "notes": "Threshold Reject. Validates typed-tool registration plus the exact structured toolResult rejection. Safe to run: mutates=false. Assistant sentinel prose is diagnostic only and cannot satisfy the row."
  },
  "gateway": {
    "wsEnv": "OPENCLAW_GATEWAY_WS",
    "tokenEnv": "OPENCLAW_GATEWAY_TOKEN"
  },
  "liveRunSafety": {
    "classification": "k6-runnable",
    "requiresLiveGatewayToken": true,
    "requiresTargetSessionKey": false,
    "requiresCandidateSha": true,
    "requiresExternalAgentOrToolInvocation": true,
    "sameSessionConcurrencySafe": false,
    "expectedArtifactClass": "PASS-candidate",
    "requiredReceipts": [
      "seat-readiness",
    "tool-invoke-rejected",
    "tempo-trace-json",
    "tool-trace-correlation"
    ],
    "foldRequiresReview": true,
    "notes": "Live request_compaction reject row. Prefer OPENCLAW_CREATE_DISPOSABLE_SESSION=true. Requires tools.effective registration and an authoritative role=toolResult context_threshold rejection; assistant prose cannot pass the row."
  }
}
